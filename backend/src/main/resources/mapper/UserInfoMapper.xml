<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cyx.paradegroundbackend.mapper.UserInfoMapper">

    <resultMap id="BaseResultMap" type="com.cyx.paradegroundbackend.model.entity.UserInfo">
        <id property="id" column="id"/>
        <result property="userAccount" column="userAccount"/>
        <result property="userPassword" column="userPassword"/>
        <result property="userName" column="userName"/>
        <result property="currentSalary" column="currentSalary"/>
        <result property="createTime" column="createTime"/>
        <result property="updateTime" column="updateTime"/>
        <result property="isDelete" column="isDelete"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, userAccount, userPassword, userName, currentSalary, createTime, updateTime, isDelete
    </sql>

    <select id="selectByUserAccount" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user_info
        WHERE userAccount = #{userAccount}
          AND isDelete = 0
        LIMIT 1
    </select>
</mapper>
